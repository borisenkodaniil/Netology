input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
  }

  date {
    match => ["asctime", "ISO8601"]
    target => "@timestamp"
  }
}

output {
  elasticsearch {
    hosts => [ "http://elasticsearch:9200" ]
    index => "logs_app-%{+YYYY.MM.dd}"
  }
}
